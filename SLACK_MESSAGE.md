Hey Phani — tomorrow you're starting a focused R&D sprint on BiasGuard 4.0. The goal is to have the core fixes DONE before we meet at noon.

Your mission:

1. Implement implicit bias detection (group → class mapping + coded xenophobia + generalization detection)

2. Fix the severity engine (qualifier dampening + no over-trigger on academic language)

3. Rebuild the rewrite engine (template-based + coherent + neutral)

4. Run all 32 test cases and get full PASS

Why:

BiasGuard works for explicit bias, but misses implicit bias, over-triggers severity, and the rewrite engine is currently unsafe. These three modules bring it to production-grade readiness.

How:

I've prepared a complete clean repo with ONLY the files you need and a fully structured test suite. Everything is ready for you. Just open the repo, follow the README, and knock out the three modules. This is a perfect 20-hour R&D block, but you can realistically finish the core fixes before noon.

Let's sync at 12:00.  
We're building something world-class here.

